# Monthly Habit Tracker (Vite + React)

Quick start:

```bash
cd calendar_monthly
npm install
npm run dev
```

Open http://localhost:5173

Data persistence:

- Habits list is stored in `localStorage` under key `habits`.
- Per-month entries are stored under `entries-YYYY-MM` keys.
